# Work Dock v1.1

サイト・今日のタスク・定型文・メモをまとめた業務ショートカットアプリ。無料のGitHub Pagesで動作し、外部ライブラリやサーバーは不要です。

- サイト名またはお気に入りの「開く」で別タブに移動。
- CRM・LP Note・広告管理（Ad Note）を初期登録。旧版の保存データには未登録のサイトだけ一度追加し、既存の名前やメモを保持。更新後に削除した初期リンクは復活しません。
- Work Dockから開いたサイトを、最上部の「最近使ったサイト」に最大6件表示。ブラウザ全体の閲覧履歴は読み取りません。
- 今日やることは1行入力で追加し、チェックで完了。「期限切れ・今日・明日・それ以降」で自動分類。期限切れは赤い枠と警告文で強調。完了は別枠で表示できます。
- 定型文とメモは一覧からコピー。「全文」で内容を確認・選択。
- 上部の「よく使う定型文」は1クリックコピー。★付き優先、その後はコピー回数順に最大6件表示。
- ☆でお気に入りに固定。上部からサイトを開く、本文をコピー、タスクを完了。
- 各一覧とお気に入りの「並び替え」でドラッグ、または上下ボタンで移動。タスクは同じ期限グループ内、完了分は完了内で移動。お気に入り欄は独立した並び順です。検索中は検索を解除してから並び替えます。
- 利用履歴・並び順もこのブラウザに保存し、バックアップに含めます。旧版のJSONバックアップにも対応。
- 検索はタイトル・本文・URLが対象。通常一覧は20件、お気に入りは12件ずつ追加表示。
- 削除は確認後に実行。直後の「元に戻す」で取り消せます。次の保存（サイト起動・コピーの利用履歴を含む）・再読み込み・別タブ更新で取り消しは終了。
- バックアップはJSONで保存・復元。復元は全置換で、確認が必要です。

データはこの端末・ブラウザのlocalStorage（`osanai-work-dock-v1`）にのみ保存されます。ログインや自動同期はありません。ブラウザのデータ削除前にはバックアップしてください。プライベートブラウズでは終了時に消える場合があります。登録内容をサーバーへ送信する機能はありません。実業務データは公開リポジトリに含めません。

GitHub Pagesは同じホストの他アプリとブラウザ保存領域のオリジンを共有します。専用キーを使用し、他アプリのキーは読み書きしません。

公開方法: mainブランチのルートをGitHub Pagesに指定。`.nojekyll` で静的ファイルをそのまま配信。
参考: https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site
